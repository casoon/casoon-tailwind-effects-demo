---
import DocsBase from '../../layouts/DocsBase.astro';
---

<DocsBase title="Docs – Changelog">
  <h1>Changelog</h1>
  <p class="lead">Short overview of library changes per version. For details, see the repository history.</p>

  <section class="section prose">
    <h2>Abgleich mit Bibliothek (^0.4.7)</h2>
    <ul>
      <li>Glass: <code>.glass</code>, <code>.glass-light</code>, <code>.glass-card*</code>, <code>.glass-button*</code>, <code>.glass-sheen</code>, <code>.glass-input*</code> vorhanden und dokumentiert.</li>
      <li>Orbs: <code>.bg-orbs-hero</code>, <code>.bg-orbs-subtle</code> dokumentiert; Parallax-Variante separat aufgeführt.</li>
      <li>Tokens: <code>--glass-radius</code>, <code>--glass-blur</code>, <code>--glass-border</code>, <code>--glass-bg</code> beschrieben.</li>
    </ul>

    <h2>0.4.x</h2>
    <ul>
      <li>Expanded glass variants: <code>.glass</code>, <code>.glass-card</code>, <code>.glass-card-light</code></li>
      <li>Utilities: <code>.glass-pulse</code>, <code>.glass-glow</code>, <code>.glass-sheen</code></li>
      <li>Verbesserte Gradients: <code>.text-gradient</code>, <code>.text-accent-gradient</code></li>
    </ul>

    <h2>0.3.x</h2>
    <ul>
      <li>Orbs backgrounds: <code>.bg-orbs-hero</code></li>
    </ul>
    <p class="muted">Note: This page is a curated overview. See “Migration” for breaking changes and upgrade paths.</p>
  </section>
</DocsBase>
