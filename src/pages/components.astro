---
import Base from '../layouts/Base.astro';
---

<Base title="Casoon Effects Demo - Komponenten" navTheme="dark">
  <div class="theme-light bg-orbs-subtle" style="padding-top: 6rem;">
    <div class="container-demo">
      <div style="text-align: center; margin-bottom: 4rem;">
        <h1 style="color: var(--text-primary); font-size: clamp(2.5rem, 5vw, 4rem); margin: 0 0 1.5rem; font-weight: 700;">
          Komponenten
        </h1>
        <p style="color: var(--text-secondary); font-size: clamp(1.125rem, 2.5vw, 1.5rem); margin: 0; max-width: 48rem; margin-inline: auto;">
          Entdecke alle verfügbaren Glass-Effekte und Komponenten der @casoon/tailwindcss-effects Bibliothek
        </p>
      </div>

      <div class="grid-cards">
        <div class="glass-card-theme glass-card-standard">
          <h3>Glass Card Standard</h3>
          <p>Klassische Glassmorphismus-Karte mit dunklem Design. Perfekt für Inhaltsdarstellung auf hellen Hintergründen.</p>
          <code>.glass-card</code>
          <div style="margin-top: 1.5rem;">
            <button class="glass-button">Neutral</button>
            <button class="glass-button-primary">Primary</button>
          </div>
        </div>

        <div class="glass-card-theme glass-card-light">
          <h3>Glass Card Light</h3>
          <p>Helle Variante für bessere Lesbarkeit auf dunklen Hintergründen. Ideal für Formulare und Eingabefelder.</p>
          <code>.glass-card-light</code>
          <div style="margin-top: 1.5rem;">
            <input type="text" placeholder="Eingabefeld..." class="glass-input-light">
          </div>
        </div>

        <div class="glass-card-theme glass-slide-card">
          <h3>Glass Slide Card</h3>
          <p>Interaktive Karte mit Slide-Funktionalität. Perfekt für Präsentationen und dynamische Inhalte.</p>
          <code>.glass-slide-card</code>
          <div class="slide-content" style="margin-top: 1.5rem;">
            <div class="slide-indicator">
              <div class="slide-dot active"></div>
              <div class="slide-dot"></div>
              <div class="slide-dot"></div>
            </div>
            <div class="slide-progress">
              <div class="slide-progress-bar"></div>
            </div>
            <div style="display: flex; gap: 0.5rem; justify-content: center;">
              <button class="slide-btn prev">←</button>
              <button class="slide-btn next">→</button>
            </div>
          </div>
        </div>
      </div>

      <div style="margin-top: 4rem; text-align: center;">
        <h2 style="color: var(--text-primary); font-size: 2rem; margin: 0 0 1.5rem;">
          Verfügbare CSS-Klassen
        </h2>
        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; max-width: 48rem; margin-inline: auto;">
          <div style="text-align: left;">
            <h3 style="color: var(--text-primary); margin: 0 0 0.75rem;">Glassmorphismus</h3>
            <ul style="color: var(--text-secondary); margin: 0; padding-left: 1.5rem;">
              <li><code>.glass-card</code></li>
              <li><code>.glass-card-light</code></li>
              <li><code>.glass-button</code></li>
              <li><code>.glass-input</code></li>
            </ul>
          </div>
          <div style="text-align: left;">
            <h3 style="color: var(--text-primary); margin: 0 0 0.75rem;">Hintergrund-Effekte</h3>
            <ul style="color: var(--text-secondary); margin: 0; padding-left: 1.5rem;">
              <li><code>.bg-orbs-hero</code></li>
              <li><code>.bg-orbs-subtle</code></li>
              <li><code>.bg-gradient</code></li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>
</Base>
